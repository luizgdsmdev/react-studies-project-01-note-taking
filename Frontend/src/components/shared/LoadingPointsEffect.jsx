/**
 * @description Loading points effect component, rendering for buttons effect (eg: loading state on create note)
 * @returns {JSX.Element} Loading points effect
 */
const LoadingPointsEffect = () => {
  return <span className="loading loading-dots loading-lg"></span>;
};

export default LoadingPointsEffect;
